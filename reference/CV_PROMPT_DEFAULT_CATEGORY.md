# The category a prompt gets when the user does not choose one.

The category a prompt gets when the user does not choose one.

## Usage

``` r
CV_PROMPT_DEFAULT_CATEGORY
```

## Format

An object of class `character` of length 1.
