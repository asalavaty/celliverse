# Degraded single-group markers from a DatasetMarkers (pooled, no per-cluster).

Degraded single-group markers from a DatasetMarkers (pooled, no
per-cluster).

## Usage

``` r
cv_datasetmarkers_pooled_lists(dm, n_markers = 20L, use_neg_markers = FALSE)
```
