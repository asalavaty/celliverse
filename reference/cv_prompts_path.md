# Path to the saved-prompts file.

One file, not one per session: these are the user's own shortcuts and
the whole point is that they outlive the conversation that created them.

## Usage

``` r
cv_prompts_path()
```
