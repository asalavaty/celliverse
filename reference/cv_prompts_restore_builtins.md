# Put every hidden built-in back.

The way out of "I removed the starters and now I want them". Leaves the
user's own prompts untouched.

## Usage

``` r
cv_prompts_restore_builtins(store = cv_prompts_load())
```
