# Narrow a marker bundle to a subset of cluster ids, preserving its shape.

Narrow a marker bundle to a subset of cluster ids, preserving its shape.

## Usage

``` r
cv_cellmarkup_subset_markers(markers, keep)
```
