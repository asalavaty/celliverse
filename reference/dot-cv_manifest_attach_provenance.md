# Attach object provenance (handle/type/summary/source) to a file entry.

Attach object provenance (handle/type/summary/source) to a file entry.

## Usage

``` r
.cv_manifest_attach_provenance(
  entry,
  filename,
  fname_to_handle,
  state,
  desc_by_handle
)
```
