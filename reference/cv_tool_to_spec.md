# Convert a single tool to a provider-neutral function spec (OpenAI-style; adapters reshape as needed).

Convert a single tool to a provider-neutral function spec (OpenAI-style;
adapters reshape as needed).

## Usage

``` r
cv_tool_to_spec(tool)
```
