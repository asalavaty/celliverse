# Build the chat messages for annotation. Forces strict JSON output.

Build the chat messages for annotation. Forces strict JSON output.

## Usage

``` r
cv_cellmarkup_prompt(markers, tissue, condition, species, top_k)
```
