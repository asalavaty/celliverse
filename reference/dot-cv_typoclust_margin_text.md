# The margin between rank-1 and rank-2 for one set, as a compact suffix.

The margin between rank-1 and rank-2 for one set, as a compact suffix.

## Usage

``` r
.cv_typoclust_margin_text(df)
```

## Arguments

- df:

  one set's candidate data.frame.

## Value

"" when there is nothing to say, else " \[...\]".
