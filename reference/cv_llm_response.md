# Standard normalized response constructor

Standard normalized response constructor

## Usage

``` r
cv_llm_response(
  content = NULL,
  tool_calls = NULL,
  finish_reason = "stop",
  raw = NULL,
  usage = NULL
)
```
