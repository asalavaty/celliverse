# Fraction of total RAM a single R process may reasonably plan to use.

Fraction of total RAM a single R process may reasonably plan to use.

## Usage

``` r
CV_MEMORY_BUDGET_FRACTION
```

## Format

An object of class `numeric` of length 1.
