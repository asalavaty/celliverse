# Curated phrase -\> tool aliases.

Regexes, anchored on whole words. Kept in one place so the list can be
read as a list rather than reverse-engineered from a matcher.

## Usage

``` r
.cv_tool_aliases()
```
