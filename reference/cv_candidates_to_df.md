# Convert a list of candidate dicts into a ranked data.frame with a CellType col.

Convert a list of candidate dicts into a ranked data.frame with a
CellType col.

## Usage

``` r
cv_candidates_to_df(cands, top_k)
```
