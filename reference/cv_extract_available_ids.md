# All annotatable set ids for an object (major +, when want_sub, sub). Used to build the "Available set(s)" list in the dropped-sets note.

All annotatable set ids for an object (major +, when want_sub, sub).
Used to build the "Available set(s)" list in the dropped-sets note.

## Usage

``` r
cv_extract_available_ids(object, want_sub)
```
