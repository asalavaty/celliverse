# Every prompt the rail should show: built-ins minus hidden, then the user's.

Order is deliberate. The starters come first because on a fresh install
they are all there is, and a user who has added their own has, by
definition, already found the rail.

## Usage

``` r
cv_prompts_all(store = cv_prompts_load())
```
