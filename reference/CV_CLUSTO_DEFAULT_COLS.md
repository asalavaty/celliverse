# Column names \`addClustoData\` writes by default.

Column names \`addClustoData\` writes by default.

## Usage

``` r
CV_CLUSTO_DEFAULT_COLS
```

## Format

An object of class `character` of length 2.
