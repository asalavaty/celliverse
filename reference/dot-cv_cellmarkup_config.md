# Build a cv_chat()-compatible config from explicit ceLLMarkup arguments.

Build a cv_chat()-compatible config from explicit ceLLMarkup arguments.

## Usage

``` r
.cv_cellmarkup_config(provider, model, api_key, host, temperature)
```
