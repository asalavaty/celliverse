# Flatten a MarkerPurity's per-type/per-group tables into one compact record.

Flatten a MarkerPurity's per-type/per-group tables into one compact
record.

## Usage

``` r
.cv_markerpurity_findings(by_type, group_ids)
```
