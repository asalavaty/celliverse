# Is an HDF5 reader available in THIS R installation? \`hdf5r\` is a Seurat Suggests, so it may or may not be present; CelliVerse never requires it.

Is an HDF5 reader available in THIS R installation? \`hdf5r\` is a
Seurat Suggests, so it may or may not be present; CelliVerse never
requires it.

## Usage

``` r
cv_has_hdf5()
```
