# Pick an existing UMAP-like reduction name from a Seurat object's reduction list, or NA if none exists.

Pick an existing UMAP-like reduction name from a Seurat object's
reduction list, or NA if none exists.

## Usage

``` r
.cv_umap_pick_existing(reds)
```
