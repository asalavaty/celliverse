# Parse the model's JSON annotation; NULL when unparseable (triggers retry).

Parse the model's JSON annotation; NULL when unparseable (triggers
retry).

## Usage

``` r
.cv_cellmarkup_parse(text, cluster_names, top_k = 3L)
```
