# Apply a parsed marker-filter spec to a single per-set marker data.frame.

Apply a parsed marker-filter spec to a single per-set marker data.frame.

## Usage

``` r
.cv_safe_head_n(n, default = 10L)
```
