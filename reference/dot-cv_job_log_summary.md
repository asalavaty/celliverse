# One-line description of a job log file, for the Results manifest.

Round LXXV (audit \#31). Deliberately says what the file IS and where it
came from, because the alternative the user sees is a bare
\`job_a1b2c3.log\`.

## Usage

``` r
.cv_job_log_summary(filename)
```
