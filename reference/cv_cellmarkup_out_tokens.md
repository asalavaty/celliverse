# Tokens the model must GENERATE for one cluster's annotation record: top_k candidates, each a cell_type + confidence + one-line reason, plus wrapping.

Tokens the model must GENERATE for one cluster's annotation record:
top_k candidates, each a cell_type + confidence + one-line reason, plus
wrapping.

## Usage

``` r
cv_cellmarkup_out_tokens(top_k)
```
