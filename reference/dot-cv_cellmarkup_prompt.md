# Build the ceLLMarkup chat messages (strict JSON out).

Build the ceLLMarkup chat messages (strict JSON out).

## Usage

``` r
.cv_cellmarkup_prompt(
  markers,
  sample_source,
  feature_type,
  tissue,
  condition,
  species,
  top_k
)
```
