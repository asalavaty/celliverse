# Per-cluster markers directly from a ClustoCell's marker tree.

Per-cluster markers directly from a ClustoCell's marker tree.

## Usage

``` r
cv_clustocell_marker_lists(
  cc,
  n_markers = 20L,
  use_neg_markers = FALSE,
  annotate_subclusters = FALSE
)
```
