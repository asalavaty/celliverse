# Registry metadata for the client (Package Browser / Tool Inspector)

Registry metadata for the client (Package Browser / Tool Inspector)

## Usage

``` r
cv_registry_metadata(reg = cv_registry())
```
