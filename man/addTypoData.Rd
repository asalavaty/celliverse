% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addTypoData.R
\name{addTypoData}
\alias{addTypoData}
\title{Add TypoClust cell type annotations to a single-cell object}
\usage{
addTypoData(
  obj,
  typoClust,
  clusters,
  rank_thresh = 1,
  refine = TRUE,
  refine_thresh = 1,
  outNames = NULL
)
}
\arguments{
\item{obj}{An object of class \code{Seurat} or \code{SingleCellExperiment}.}

\item{typoClust}{An object of class \code{TypoClust}, generated using \code{typoClust()}.}

\item{clusters}{Character vector; names of metadata columns in \code{obj} defining clusters
or cell subsets to which cell types will be assigned.}

\item{rank_thresh}{Integer; the top N ranked cell types to add for each cluster, stored as
separate metadata columns.}

\item{refine}{Logical; whether to refine inferred cell types by traversing deeper levels
of the cell type hierarchy.}

\item{refine_thresh}{Integer; depth of (lexical) hierarchical traversal for refinement. Ignored if
\code{refine = FALSE}.}

\item{outNames}{Character vector; names of output metadata columns. If \code{NULL}, defaults
to \code{paste0(clusters, "_Celltype")}.}
}
\value{
The input object \code{obj} with additional metadata columns containing
inferred cell type annotations.
}
\description{
Adds inferred cell type annotations from a \code{TypoClust} object to the
metadata of a Seurat or SingleCellExperiment object.
}
\details{
Cell type labels are assigned to specified cluster or subset columns and
appended as new metadata columns. Multiple ranked cell types can be added,
and hierarchical refinement can be applied to obtain more specific cell
type annotations.
}
\examples{
\dontrun{
so <- addTypoData(
  obj = so,
  typoClust = tc,
  clusters = "ClustoCell_Clusters"
)
}

}
\seealso{
\code{\link{typoClust}}, \code{\link{typoClustVis}}
}
