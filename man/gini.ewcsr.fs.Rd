% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gini.ewcsr.fs.R
\name{gini.ewcsr.fs}
\alias{gini.ewcsr.fs}
\title{Feature selection using Gini coefficient on EWCSR-transformed data}
\usage{
gini.ewcsr.fs(
  mat,
  gini_thresh = 0.5,
  ewcsr_high_thresh = NULL,
  ewcsr_low_thresh = NULL,
  noise_thresh = NULL,
  num_threads = -1
)
}
\arguments{
\item{mat}{A matrix with features as rows and cells as columns.}

\item{gini_thresh}{Numeric; Gini threshold for selecting specific features.}

\item{ewcsr_high_thresh}{Numeric; upper EWCSR threshold for binarization. EWCSR values higher than this threshold will be converted to TRUE.}

\item{ewcsr_low_thresh}{Numeric; lower EWCSR threshold for binarization. EWCSR values lower than this threshold will be converted to TRUE.}

\item{noise_thresh}{Integer; minimum number of cells required for a feature to be retained.}

\item{num_threads}{Integer; number of threads to use. \code{-1} uses all available cores.}
}
\value{
A list containing \code{specific_features}, \code{non_specific_features},
and \code{no_occurrence}.
}
\description{
Performs feature selection based on the Gini inequality coefficient computed
on expression-weighted centered scaled rank (EWCSR) data.
}
\details{
Features are categorized into specific, non-specific, and no-occurrence
groups based on Gini thresholds and optional binarization of EWCSR values.
}
\examples{
utils::data("pbmc_small", package = "SeuratObject")

mat <- SeuratObject::LayerData(
  pbmc_small,
  assay = "RNA",
  layer = "counts"
)

fs <- gini.ewcsr.fs(
  mat,
  num_threads = 1
)

}
\seealso{
\code{\link{ewcsr.sparse}}, \code{\link{gini.rank.fs}}
}
