% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent_tools_cellmarkup.R
\name{cv_resolve_set_ids_lenient}
\alias{cv_resolve_set_ids_lenient}
\title{Lenient set-id resolver: case/separator-insensitive match against the
available ids, returning NA for ids with no unique match (so the caller can
report all unknowns at once). Unlike cv_resolve_set_ids it never aborts.}
\usage{
cv_resolve_set_ids_lenient(requested, available)
}
\description{
Lenient set-id resolver: case/separator-insensitive match against the
available ids, returning NA for ids with no unique match (so the caller can
report all unknowns at once). Unlike cv_resolve_set_ids it never aborts.
}
\keyword{internal}
