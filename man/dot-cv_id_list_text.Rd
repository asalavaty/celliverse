% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent_object_store.R
\name{.cv_id_list_text}
\alias{.cv_id_list_text}
\title{Render an id list for a summary line, announcing any truncation.}
\usage{
.cv_id_list_text(ids, total = NULL, max_show = NULL)
}
\arguments{
\item{ids}{the ids the descriptor actually holds (already bounded).}

\item{total}{the TRUE number of ids the object has. Compared against
`length(ids)`, so the "+N more" marker is measured against reality rather
than against the list that was already cut -- the bug this replaces.}

\item{max_show}{optional further cap for display; NULL shows everything the
descriptor holds, which is the default because the model is instructed to
take set ids from here.}
}
\value{
a bracketed string like " [C1, C2, C3 ... (+7 more)]", or "".
}
\description{
Render an id list for a summary line, announcing any truncation.
}
\keyword{internal}
