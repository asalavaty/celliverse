% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent_bigdata.R
\name{cv_heavy_dispatch_needs_mb}
\alias{cv_heavy_dispatch_needs_mb}
\title{The memory a heavy dispatch on an already-resident object needs, in MB.}
\usage{
cv_heavy_dispatch_needs_mb(bytes, n_cells_used, n_cells_total)
}
\arguments{
\item{bytes}{the object's own resident size (`cv_object_bytes()`).}

\item{n_cells_used}{how many cells the dispatch will actually cluster (the
sketch size, or the total when not sketching).}

\item{n_cells_total}{the object's total cell count.}
}
\value{
numeric MB, or `NA_real_` when `bytes` cannot be measured.
}
\description{
Two terms, not one:
}
\details{
FLOOR  -- `cv_heavy_dispatch_floor_mb(bytes)`. Paid on the FULL object
    (`n_cells_total`), before any sketch is taken, so no `sketch_ncells`
    reduces it.
  SCALED -- `bytes * CV_SEURAT_EXTRA_FACTOR * (n_cells_used / n_cells_total)`.
    The final clustering step, sized to however many cells are actually
    used -- all of them, or the sketch. This is the term `sketch_ncells`
    actually buys down.
}
\keyword{internal}
